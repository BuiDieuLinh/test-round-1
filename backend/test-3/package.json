{
  "name": "restaurant-booking-demo",
  "version": "1.0.0",
  "description": "Demo product for Restaurant Table Booking system with Prisma ORM and SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:init": "npx prisma db push && npx prisma db seed",
    "prisma:studio": "npx prisma studio",
    "test": "node test.js"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
